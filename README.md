# 廃棄層の発掘家 v19.0.0

核戦争後の世界。AIロボットが作り、捨て続ける「廃棄層」に潜り、
集落の浄水機関を動かす燃料「アクアコア」(地下30階)を持ち帰る
シレン/トルネコ系のターン制ローグライク。

## v19.0.0 の変更点(画像アセット導入)

v18.0.0 のシステムはそのままに、**あなたの高品質イラストを全面採用**しました。

- キャラ・敵・NPCを**立ち絵**として描画(足元に影、地面に立つ見た目)
- 歩行は「揺れ」ではなく、**上下バウンド+影の伸縮+踏み込みモーション**
- ダンジョンの床・壁・特殊オブジェクトをイラストタイルで描画
- 集落とタイトルにイラスト背景を採用
- 立ち絵は足元Y座標で奥行きソート(手前のキャラが奥に重なる)
- 画像が読めない場合はコード生成スプライトに自動フォールバック(堅牢)

### アセットの最適化

元の22MBの画像を**約0.9MBに圧縮**しました(スマホ・GitHub Pages快適化)。

- 立ち絵: 余白を切り、足元基準・中心揃えで正規化(208x256, 64色)
- タイル: 512→128px(128色)、装飾は96px
- 背景: 幅1000pxのJPEG(品質82)
- アイテム種別アイコン: 80px(64色)

## ファイル構成

```
index.html / style.css
js/   … rng, config, assets, sprites, audio, dungeon, ui, input, game
assets/
  chars/    … 立ち絵(主人公・NPC・敵に流用)
  tiles/    … 床・壁・特殊オブジェクト・装飾
  items/    … 種別アイコン
  bg/       … タイトル・集落・ダンジョン背景
  fx/       … エフェクト
```

旧 v17 の `src/` は不要です(削除して構いません)。

## 操作

| 操作 | スマホ | PC |
|---|---|---|
| 移動(8方向) | 行きたい方向をタップ | 矢印 / hjkl+yubn |
| 連続移動 | 長押し | キー押しっぱなし |
| ダッシュ | 同方向を素早く2回タップ | Shift+方向 |
| 攻撃 | 敵の方向をタップ | 敵の方向へ移動 |
| 階段・拾う | 自分をタップ / 「足元」 | Enter / Space |
| 道具 | 「道具」ボタン | i |
| 足踏み | 「待機」ボタン | . または s |

## ルール(シレン系)

- 自分が1歩動くと、敵も1歩動くターン制
- 床の道具は歩くだけで自動で拾う
- 満腹度が尽きるとHPが減り続ける
- 倒れると**道具を全て失い、レベルも1に戻る**(集落へ送還)
- 「帰還タグ」で道具を持ったまま帰還できる
- 装備には強化値(+1〜)があり、腐食粘体や錆噴霧で下がる

## セキュリティ・通信について

外部通信は**一切行いません**。

- CSP: `default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data: blob:`
  (画像はすべて同一オリジンの `assets/` 配下のローカルファイル)
- fetch / XHR / WebSocket / 外部script / iframe / serviceWorker: 不使用
- 音: すべてWebAudioでコード生成

### localStorage の使用内容

| キー | 内容 |
|---|---|
| `hakkutsuka_v18_save` | セーブ(レベル・HP・持ち物・装備・クリア回数) |
| `hakkutsuka_v18_record` | 発掘記録(最深到達階・出発回数・コア回収回数) |

### 通信確認手順

1. 開発者ツール → Network タブを開く
2. 再読み込みしてプレイ
3. `index.html` / `style.css` / `js/*` / `assets/*` 以外の
   リクエストが発生しないことを確認(CSP違反はConsoleに出ます)

## デプロイ(PowerShell)

```powershell
git rm -r src           # 旧v17のsrcが残っていれば(初回のみ)
git add .
git commit -m "Update game to v19.0.0"
git push origin main
```
